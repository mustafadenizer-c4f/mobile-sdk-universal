jdk:
  - openjdk17
  
before_install:
  - chmod +x ./gradlew

install:
  - ./gradlew :surveysdk:assembleRelease
  - ./gradlew :surveysdk-react-native:assembleRelease
  - find . -name "*.aar" -ls  # Debug: List all aar files